class ComplexNumber (
    var realNumber : Double,
    var imaginaryNumber: Double
        ) {

        init {
            println("We created complex number as: $realNumber + i$imaginaryNumber");
        }
}